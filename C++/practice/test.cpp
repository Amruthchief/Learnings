#include <iostream>
#include <stack>
#include <vector>
#include <string>
#include <algorithm>


using namespace std;
void fun(int arr[])  // SAME AS void fun(int *arr) 
{ while(arr[] < sizeof(arr))
    cout<<arr;
}
  
// Driver program 
int main() 
{ 
   int arr[] = {1, 2, 3, 4, 5, 6, 7, 8}; 
  while(arr[] < sizeof(arr))
    cout<<arr;
   fun(arr); 
   return 0; 
} 